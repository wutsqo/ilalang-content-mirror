{
  "name": "ilalang-content-mirror",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run ./src/index.ts",
    "compile": "bun run ./src/compile.ts && bunx @11ty/eleventy && bun run ./src/generate-pdf.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@vercel/postgres": "^0.9.0",
    "gray-matter": "^4.0.3",
    "puppeteer": "^23.1.1"
  }
}
